apiVersion: v1
kind: Service
metadata:
  name: mcp-wiki-service
  namespace: mcp-wiki
  labels:
    app: mcp-wiki-server
spec:
  type: LoadBalancer
  selector:
    app: mcp-wiki-server
  ports:
    - name: http
      port: 80
      targetPort: 4200
      protocol: TCP